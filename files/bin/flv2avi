#!/bin/bash
source=${1}
dest=${source/flv/avi}

#ffmpeg -sameq -i ${source} -acodec libmp3lame ${dest}
ffmpeg -pass 1 -sameq -i ${source} -acodec libmp3lame -vcodec libx264 ${dest}
ffmpeg -pass 2 -sameq -i ${source} -acodec libmp3lame -vcodec libx264 ${dest}
