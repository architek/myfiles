#!/bin/bash
source=${1}
dest=${source/flv/avi}

#ffmpeg -sameq -i ${source} -acodec libmp3lame ${dest}
ffmpeg -i "${source}" -acodec libmp3lame -vcodec libx264 "${dest}" -pass 1 -qscale 0
ffmpeg -i "${source}" -acodec libmp3lame -vcodec libx264 "${dest}" -pass 2 -qscale 0
